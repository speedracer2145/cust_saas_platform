apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: roberta-sentiment-data-pvc
  namespace: roberta-sentiment
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: standard  # Adjust based on your cluster's storage class
